/**
 * Configuration system — JSON parsing, validation, and hot-reload.
 * All config classes are platform-independent, using Gson or manual parsing.
 * Hot-reload is triggered by the {@code /stylizeddamage reload} command.
 */
package com.stylizeddamage.common.config;
