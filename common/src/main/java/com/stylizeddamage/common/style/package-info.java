/**
 * Style system — color models, easing curves, animation modules, and the Style data object.
 * Each Style is a pure data record that defines visual appearance for damage numbers.
 * Styles are loaded from JSON files in the {@code config/stylizeddamage/styles/} directory.
 */
package com.stylizeddamage.common.style;
