/**
 * Animation engine — drives position, size, brightness, and opacity animations
 * for damage numbers. Supports easing curves and random value interpolation.
 * Four independent animation modules run in parallel during enter/hold/exit phases.
 */
package com.stylizeddamage.common.animation;
